{
    "name": "terremoth/php-ascii-box-drawer",
    "description": "Simple app that draws rectangles using ASCII chars",
    "type": "project",
    "require": {
        "php": "^8.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5",
        "vimeo/psalm": "^4.29"
    },
    "license": "GPL-3.0-or-later",
    "autoload": {
        "psr-4": {
            "BoxDrawer\\": "src/BoxDrawer"
        }
    },
    "autoload-dev": {
        "BoxDrawer\\Test\\": "tests/BoxDrawer"
    },
    "authors": [
        {
            "name": "terremoth",
            "email": "dutra.astro@gmail.com"
        }
    ],
    "minimum-stability": "stable",
    "scripts": {
        "fix": "phpcbf",
        "lint": "phpcs",
        "analyze": "psalm --shepherd --no-cache",
        "test": "vendor/bin/phpunit tests",
        "ci": [
            "@composer lint",
            "@composer analyze",
            "@composer test"
        ],
        "build": [
            "@composer fix",
            "@composer lint",
            "@composer analyze",
            "@composer test"
        ]
    }
}
